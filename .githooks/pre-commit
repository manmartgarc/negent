#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

go run ./internal/tools/readmecommands >/dev/null

if ! git diff --quiet -- README.md; then
	git add README.md
	echo "Updated README command table."
fi
